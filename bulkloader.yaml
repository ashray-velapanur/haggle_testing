# Autogenerated bulkloader.yaml file.
# You must edit this file before using it. TODO: Remove this line when done.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users

transformers:

- kind: Campaign
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: busy_occupancy
      external_name: busy_occupancy
      # Type: Integer Stats: 2629 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: busy_period
      external_name: busy_period
      # Type: Text Stats: 2629 properties of this type in this kind.
      import_transform: db.Text

    - property: free_occupancy
      external_name: free_occupancy
      # Type: Integer Stats: 2629 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: max_discount
      external_name: max_discount
      # Type: Float Stats: 2629 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: min_discount
      external_name: min_discount
      # Type: Float Stats: 2629 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: preference
      external_name: preference
      # Type: Integer Stats: 2629 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: vendor
      external_name: vendor
      # Type: Key Stats: 2629 properties of this type in this kind.
      import_transform: transform.create_foreign_key('Vendor')
      export_transform: transform.key_id_or_name_as_string


- kind: Checkin
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: checkin_id
      external_name: checkin_id
      # Type: String Stats: 512 properties of this type in this kind.

    - property: created_at
      external_name: created_at
      # Type: Date/Time Stats: 512 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: network
      external_name: network
      # Type: String Stats: 512 properties of this type in this kind.

    - property: user
      external_name: user
      # Type: Key Stats: 512 properties of this type in this kind.
      import_transform: transform.create_foreign_key('User')
      export_transform: transform.key_id_or_name_as_string

    - property: vendor
      external_name: vendor
      # Type: Key Stats: 512 properties of this type in this kind.
      import_transform: transform.create_foreign_key('Vendor')
      export_transform: transform.key_id_or_name_as_string


- kind: Comment
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: checkin
      external_name: checkin
      # Type: Key Stats: 240 properties of this type in this kind.
      import_transform: transform.create_foreign_key('Checkin')
      export_transform: transform.key_id_or_name_as_string

    - property: count
      external_name: count
      # Type: Integer Stats: 178 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: network
      external_name: network
      # Type: String Stats: 240 properties of this type in this kind.

    - property: text
      external_name: text
      # Type: Text Stats: 62 properties of this type in this kind.
      import_transform: db.Text

    - property: user_id
      external_name: user_id
      # Type: String Stats: 62 properties of this type in this kind.


- kind: Deal
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: bid_discount
      external_name: bid_discount
      # Type: Float Stats: 792 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: campaign
      external_name: campaign
      # Type: Key Stats: 792 properties of this type in this kind.
      import_transform: transform.create_foreign_key('Campaign')
      export_transform: transform.key_id_or_name_as_string

    - property: offered_at
      external_name: offered_at
      # Type: Date/Time Stats: 792 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: offered_discount
      external_name: offered_discount
      # Type: Float Stats: 792 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: party_size
      external_name: party_size
      # Type: Integer Stats: 792 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: redemption_datetime
      external_name: redemption_datetime
      # Type: Date/Time Stats: 792 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: scores
      external_name: scores
      # Type: Float Stats: 2865 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: status
      external_name: status
      # Type: Integer Stats: 792 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: HistoryScore
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: score
      external_name: score
      # Type: Float Stats: 386 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)


- kind: Like
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: checkin
      external_name: checkin
      # Type: Key Stats: 245 properties of this type in this kind.
      import_transform: transform.create_foreign_key('Checkin')
      export_transform: transform.key_id_or_name_as_string

    - property: count
      external_name: count
      # Type: Integer Stats: 178 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: network
      external_name: network
      # Type: String Stats: 245 properties of this type in this kind.

    - property: user_id
      external_name: user_id
      # Type: String Stats: 67 properties of this type in this kind.


- kind: Location
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: city
      external_name: city
      # Type: String Stats: 877 properties of this type in this kind.


- kind: LoyaltyScore
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: score
      external_name: score
      # Type: Float Stats: 339 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)


- kind: MaxHistory
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: score
      external_name: score
      # Type: Float Stats: 20 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)


- kind: NeighborhoodSweepResults
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: file_name
      external_name: file_name
      # Type: String Stats: 28300 properties of this type in this kind.

    - property: neighborhood
      external_name: neighborhood
      # Type: String Stats: 28300 properties of this type in this kind.

    - property: network
      external_name: network
      # Type: String Stats: 28300 properties of this type in this kind.

    - property: square
      external_name: square
      # Type: String Stats: 28300 properties of this type in this kind.


- kind: Neighborhood
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: city
      external_name: city
      # Type: String Stats: 3 properties of this type in this kind.

    - property: seed_pt
      external_name: seed_pt
      # Type: GeoPt Stats: 3 properties of this type in this kind.

    - property: squares
      external_name: squares
      # Type: String Stats: 14216 properties of this type in this kind.


- kind: NotificationSubscribers
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: comments
      external_name: comments
      # Type: Text Stats: 6 properties of this type in this kind.
      import_transform: db.Text

    - property: email
      external_name: email
      # Type: String Stats: 6 properties of this type in this kind.

    # Warning: This property is a duplicate, but with a different type.
    # TODO: Edit this transform so only one property with this name remains.
    #- property: email
    #  external_name: email
      # Type: Text Stats: 4 properties of this type in this kind.
    #  import_transform: db.Text

    - property: first_name
      external_name: first_name
      # Type: String Stats: 6 properties of this type in this kind.

    - property: last_name
      external_name: last_name
      # Type: String Stats: 6 properties of this type in this kind.

    - property: name
      external_name: name
      # Type: Text Stats: 38 properties of this type in this kind.
      import_transform: db.Text

    - property: restaurant_name
      external_name: restaurant_name
      # Type: String Stats: 6 properties of this type in this kind.

    - property: user_type
      external_name: user_type
      # Type: String Stats: 6 properties of this type in this kind.


- kind: Occupancy
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: busy_period
      external_name: busy_period
      # Type: Text Stats: 693 properties of this type in this kind.
      import_transform: db.Text

    - property: key_
      external_name: key_
      # Type: String Stats: 693 properties of this type in this kind.

    - property: preference
      external_name: preference
      # Type: Integer Stats: 693 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: remaining
      external_name: remaining
      # Type: Integer Stats: 1179 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: vendor
      external_name: vendor
      # Type: Key Stats: 693 properties of this type in this kind.
      import_transform: transform.create_foreign_key('Vendor')
      export_transform: transform.key_id_or_name_as_string


- kind: PurchasingPowerScore
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: score
      external_name: score
      # Type: Float Stats: 16 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)


- kind: SocialInfluenceScore
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: score
      external_name: score
      # Type: Float Stats: 2010 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)


- kind: Tags
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: tags
      external_name: tags
      # Type: String Stats: 5 properties of this type in this kind.

    - property: vendor
      external_name: vendor
      # Type: Key Stats: 5 properties of this type in this kind.
      import_transform: transform.create_foreign_key('Vendor')
      export_transform: transform.key_id_or_name_as_string


- kind: ThirdPartyUserData
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: _access_token
      external_name: _access_token
      # Type: String Stats: 12 properties of this type in this kind.

    - property: access_token
      external_name: access_token
      # Type: String Stats: 18 properties of this type in this kind.

    - property: id_
      external_name: id_
      # Type: String Stats: 28 properties of this type in this kind.

    - property: last_updated
      external_name: last_updated
      # Type: Date/Time Stats: 25 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: likes
      external_name: likes
      # Type: Key Stats: 36 properties of this type in this kind.
      import_transform: transform.create_foreign_key('Likes')
      export_transform: transform.key_id_or_name_as_string

    - property: token_secret
      external_name: token_secret
      # Type: String Stats: 10 properties of this type in this kind.


- kind: ThirdPartyVendorData
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: category
      external_name: category
      # Type: String Stats: 33810 properties of this type in this kind.

    - property: city
      external_name: city
      # Type: String Stats: 33810 properties of this type in this kind.

    - property: id_
      external_name: id_
      # Type: String Stats: 33815 properties of this type in this kind.

    - property: last_updated
      external_name: last_updated
      # Type: Date/Time Stats: 33815 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

#    - property: location_
#      external_name: location_
#      # Type: GeoPt Stats: 33810 properties of this type in this kind.

    - property: name
      external_name: name
      # Type: String Stats: 33810 properties of this type in this kind.

    - property: network
      external_name: network
      # Type: String Stats: 33815 properties of this type in this kind.


- kind: Tip
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: sentiment
      external_name: sentiment
      # Type: Integer Stats: 35 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: text
      external_name: text
      # Type: Text Stats: 38 properties of this type in this kind.
      import_transform: db.Text

    - property: user
      external_name: user
      # Type: Key Stats: 38 properties of this type in this kind.
      import_transform: transform.create_foreign_key('User')
      export_transform: transform.key_id_or_name_as_string

    - property: vendor
      external_name: vendor
      # Type: Key Stats: 35 properties of this type in this kind.
      import_transform: transform.create_foreign_key('Vendor')
      export_transform: transform.key_id_or_name_as_string


- kind: UserVendor
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: loyalty_score
      external_name: loyalty_score
      # Type: Float Stats: 338 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: user
      external_name: user
      # Type: Key Stats: 338 properties of this type in this kind.
      import_transform: transform.create_foreign_key('User')
      export_transform: transform.key_id_or_name_as_string

    - property: vendor
      external_name: vendor
      # Type: Key Stats: 338 properties of this type in this kind.
      import_transform: transform.create_foreign_key('Vendor')
      export_transform: transform.key_id_or_name_as_string


- kind: User
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: email
      external_name: email
      # Type: String Stats: 31 properties of this type in this kind.

    - property: force_reset_password
      external_name: force_reset_password
      # Type: Boolean Stats: 2 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: last_updated_pipeline
      external_name: last_updated_pipeline
      # Type: String Stats: 29 properties of this type in this kind.

    - property: name
      external_name: name
      # Type: String Stats: 31 properties of this type in this kind.

    - property: password
      external_name: password
      # Type: String Stats: 2 properties of this type in this kind.

    - property: total_checkins
      external_name: total_checkins
      # Type: Float Stats: 30 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: total_followers
      external_name: total_followers
      # Type: Integer Stats: 16 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: total_friends
      external_name: total_friends
      # Type: Integer Stats: 30 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: VendorAdmin
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: role
      external_name: role
      # Type: Integer Stats: 108 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: vendor
      external_name: vendor
      # Type: Key Stats: 108 properties of this type in this kind.
      import_transform: transform.create_foreign_key('Vendor')
      export_transform: transform.key_id_or_name_as_string


- kind: Vendor
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

#    - property: _dollar_rating
#      external_name: _dollar_rating
      # Type: Integer Stats: 4733 properties of this type in this kind.
#      import_transform: transform.none_if_empty(int)

#    - property: _operating_hours
#      external_name: _operating_hours
      # Type: Text Stats: 24229 properties of this type in this kind.
#      import_transform: db.Text

    - property: category
      external_name: category
      # Type: Key Stats: 24229 properties of this type in this kind.
      import_transform: transform.create_foreign_key('Category')
      export_transform: transform.key_id_or_name_as_string

    - property: category_name
      external_name: category_name
      # Type: String Stats: 24226 properties of this type in this kind.

#    - property: location
#      external_name: location
      # Type: GeoPt Stats: 24226 properties of this type in this kind.

#    - property: max_loyalty_score
#      external_name: max_loyalty_score
      # Type: Float Stats: 24229 properties of this type in this kind.
#      import_transform: transform.none_if_empty(float)

#    - property: name
#      external_name: name
      # Type: String Stats: 24229 properties of this type in this kind.

#    - property: neighborhoods
#      external_name: neighborhoods
      # Type: String Stats: 28998 properties of this type in this kind.

#    - property: photo
#      external_name: photo
      # Type: String Stats: 48 properties of this type in this kind.

#    - property: user_rating
#      external_name: user_rating
      # Type: Float Stats: 24226 properties of this type in this kind.
#      import_transform: transform.none_if_empty(float)

#    - property: vendor_code
#      external_name: vendor_code
      # Type: String Stats: 95 properties of this type in this kind.

#    - property: vendor_location
#      external_name: vendor_location
      # Type: Key Stats: 24229 properties of this type in this kind.
#      import_transform: transform.create_foreign_key('Location')
#      export_transform: transform.key_id_or_name_as_string


- kind: X
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: x
      external_name: x
      # Type: Integer Stats: 3152 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: _AE_Backup_Information_Kind_Files
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: files
      external_name: files
      # Type: String Stats: 105 properties of this type in this kind.


- kind: _AE_Backup_Information_Kind_Type_Info
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: entity_type_info
      external_name: entity_type_info
      # Type: Text Stats: 447 properties of this type in this kind.
      import_transform: db.Text

    - property: is_partial
      external_name: is_partial
      # Type: Boolean Stats: 446 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)


- kind: _AE_Backup_Information
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: complete_time
      external_name: complete_time
      # Type: Date/Time Stats: 4 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: completed_jobs
      external_name: completed_jobs
      # Type: String Stats: 26 properties of this type in this kind.

    - property: filesystem
      external_name: filesystem
      # Type: String Stats: 4 properties of this type in this kind.

    - property: kinds
      external_name: kinds
      # Type: String Stats: 26 properties of this type in this kind.

    - property: name
      external_name: name
      # Type: String Stats: 4 properties of this type in this kind.

    - property: start_time
      external_name: start_time
      # Type: Date/Time Stats: 4 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')


- kind: _AE_DatastoreAdmin_Operation
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: active_job_ids
      external_name: active_job_ids
      # Type: String Stats: 9 properties of this type in this kind.

    - property: active_jobs
      external_name: active_jobs
      # Type: Integer Stats: 28 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: completed_jobs
      external_name: completed_jobs
      # Type: Integer Stats: 28 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: description
      external_name: description
      # Type: Text Stats: 28 properties of this type in this kind.
      import_transform: db.Text

    - property: last_updated
      external_name: last_updated
      # Type: Date/Time Stats: 28 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: status
      external_name: status
      # Type: String Stats: 28 properties of this type in this kind.

    - property: status_info
      external_name: status_info
      # Type: String Stats: 28 properties of this type in this kind.


- kind: _AE_Pipeline_Barrier
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: blocking_slots
      external_name: blocking_slots
      # Type: Key Stats: 74171 properties of this type in this kind.
      import_transform: transform.create_foreign_key('BLOCKINGSLOTS')
      export_transform: transform.key_id_or_name_as_string

    - property: root_pipeline
      external_name: root_pipeline
      # Type: Key Stats: 51581 properties of this type in this kind.
      import_transform: transform.create_foreign_key('ROOTPIPELINE')
      export_transform: transform.key_id_or_name_as_string

    - property: status
      external_name: status
      # Type: String Stats: 51581 properties of this type in this kind.

    - property: target
      external_name: target
      # Type: Key Stats: 51581 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TARGET')
      export_transform: transform.key_id_or_name_as_string

    - property: trigger_time
      external_name: trigger_time
      # Type: Date/Time Stats: 51396 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')


- kind: _AE_Pipeline_Record
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: abort_message
      external_name: abort_message
      # Type: Text Stats: 76 properties of this type in this kind.
      import_transform: db.Text

    - property: class_path
      external_name: class_path
      # Type: String Stats: 45862 properties of this type in this kind.

    - property: current_attempt
      external_name: current_attempt
      # Type: Integer Stats: 45862 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: fanned_out
      external_name: fanned_out
      # Type: Key Stats: 40008 properties of this type in this kind.
      import_transform: transform.create_foreign_key('FANNEDOUT')
      export_transform: transform.key_id_or_name_as_string

    - property: finalized_time
      external_name: finalized_time
      # Type: Date/Time Stats: 45846 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: is_root_pipeline
      external_name: is_root_pipeline
      # Type: Boolean Stats: 5854 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: max_attempts
      external_name: max_attempts
      # Type: Integer Stats: 45862 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: next_retry_time
      external_name: next_retry_time
      # Type: Date/Time Stats: 89 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: params
      external_name: params
      # Type: Text Stats: 45862 properties of this type in this kind.
      import_transform: db.Text

    - property: retry_message
      external_name: retry_message
      # Type: Text Stats: 89 properties of this type in this kind.
      import_transform: db.Text

    - property: root_pipeline
      external_name: root_pipeline
      # Type: Key Stats: 45862 properties of this type in this kind.
      import_transform: transform.create_foreign_key('ROOTPIPELINE')
      export_transform: transform.key_id_or_name_as_string

    - property: start_time
      external_name: start_time
      # Type: Date/Time Stats: 40143 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: status
      external_name: status
      # Type: String Stats: 45862 properties of this type in this kind.


- kind: _AE_Pipeline_Slot
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: fill_time
      external_name: fill_time
      # Type: Date/Time Stats: 28359 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: filler
      external_name: filler
      # Type: Key Stats: 28359 properties of this type in this kind.
      import_transform: transform.create_foreign_key('FILLER')
      export_transform: transform.key_id_or_name_as_string

    - property: root_pipeline
      external_name: root_pipeline
      # Type: Key Stats: 28444 properties of this type in this kind.
      import_transform: transform.create_foreign_key('ROOTPIPELINE')
      export_transform: transform.key_id_or_name_as_string

    - property: status
      external_name: status
      # Type: String Stats: 28444 properties of this type in this kind.

    - property: value
      external_name: value
      # Type: Text Stats: 28359 properties of this type in this kind.
      import_transform: db.Text


- kind: _GAE_MR_MapreduceControl
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: command
      external_name: command
      # Type: Text Stats: 9 properties of this type in this kind.
      import_transform: db.Text


- kind: _GAE_MR_MapreduceState
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: aborted_shards
      external_name: aborted_shards
      # Type: Integer Stats: 9 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: active
      external_name: active
      # Type: Boolean Stats: 9 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: active_shards
      external_name: active_shards
      # Type: Integer Stats: 9 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: chart_url
      external_name: chart_url
      # Type: Text Stats: 9 properties of this type in this kind.
      import_transform: db.Text

    - property: chart_width
      external_name: chart_width
      # Type: Integer Stats: 9 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: counters_map
      external_name: counters_map
      # Type: Text Stats: 9 properties of this type in this kind.
      import_transform: db.Text

    - property: failed_shards
      external_name: failed_shards
      # Type: Integer Stats: 9 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: last_poll_time
      external_name: last_poll_time
      # Type: Date/Time Stats: 9 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: mapreduce_spec
      external_name: mapreduce_spec
      # Type: Text Stats: 9 properties of this type in this kind.
      import_transform: db.Text

    - property: result_status
      external_name: result_status
      # Type: String Stats: 9 properties of this type in this kind.

    - property: sparkline_url
      external_name: sparkline_url
      # Type: Text Stats: 9 properties of this type in this kind.
      import_transform: db.Text

    - property: start_time
      external_name: start_time
      # Type: Date/Time Stats: 9 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: writer_state
      external_name: writer_state
      # Type: Text Stats: 9 properties of this type in this kind.
      import_transform: db.Text

